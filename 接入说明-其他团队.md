# QX Hybrid Android SDK 接入说明

SDK 已发布到公司 GitLab 的 `android-sdk-maven` 包仓库,内网即可拉取。

- **依赖坐标:** `com.energy.sdk:qx-hybrid:0.1.8`
- **仓库项目:** https://paas-gitlab.mychery.com/20238591/android-sdk-maven (Project ID: 11998)

---

## 一、准备:GitLab 读取令牌

1. 找 SDK 维护者把你加进 `android-sdk-maven` 项目成员(至少 Reporter/读权限)。
2. 生成一个 Personal Access Token:GitLab 头像 → **Preferences → Access Tokens**,
   scope 勾 `read_api`(或 `read_package_registry`)。复制保存好这个 token。

> 建议把 token 放到本机 `~/.gradle/gradle.properties`(不要提交到 git):
> ```properties
> gitlabMavenToken=<你的 PAT>
> ```

## 二、添加仓库

在你工程的 `settings.gradle` 的 `dependencyResolutionManagement { repositories { ... } }` 里加:

```groovy
// QX Hybrid SDK(公司 GitLab 包仓库)
maven {
    url 'https://paas-gitlab.mychery.com/api/v4/projects/11998/packages/maven'
    credentials(HttpHeaderCredentials) {
        name  = 'Private-Token'
        value = providers.gradleProperty('gitlabMavenToken').getOrElse('')
    }
    authentication { header(HttpHeaderAuthentication) }
}

// SDK 的传递依赖 Android-BLE 在 jitpack:
maven { url 'https://jitpack.io' }

// 其余传递依赖来源(一般项目已有):
google()
mavenCentral()
```

## 三、添加依赖

模块 `build.gradle`:

```groovy
dependencies {
    implementation 'com.energy.sdk:qx-hybrid:0.1.8'
}
```

Sync 即可。以下传递依赖会自动带入,无需手动声明:
`androidx.appcompat`、`com.google.android.material`、`androidx.core:core-ktx`、
`androidx.webkit:webkit`、`androidx.exifinterface`、`com.google.zxing:core`、
`com.journeyapps:zxing-android-embedded`、`com.github.aicareles:Android-BLE`、
`com.google.code.gson:gson`、`kotlin-stdlib`。

---

## 常见问题

- **401 / 拉不到包**:token 无效或没权限。确认已被加进 `android-sdk-maven` 项目、PAT 有 `read_api` scope。
- **Android-BLE 拉不到**:你的网络访问不了公网 jitpack,联系 SDK 维护者把该依赖也镜像到公司仓库。
- **版本升级**:把坐标里的 `0.1.8` 换成维护者通知的新版本号即可。
