# QX Hybrid Android SDK 接入说明

SDK 以 **Maven 仓库目录**的形式发布在 GitHub 上,直接用 raw 地址拉取即可,**不需要内网、不需要 token**。

- **依赖坐标:** `com.energy.sdk:qx-hybrid:0.1.11`
- **仓库地址:** https://github.com/gu0315/qx-hybrid-maven

---

## 一、添加仓库

在你工程的 `settings.gradle` 的 `dependencyResolutionManagement { repositories { ... } }` 里加:

```groovy
dependencyResolutionManagement {
    repositories {
        // ① 本 SDK 仓库
        maven { url 'https://raw.githubusercontent.com/gu0315/qx-hybrid-maven/main' }

        // ② SDK 的传递依赖来源 —— 必须有,否则部分依赖拉不到
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }   // Android-BLE(com.github.aicareles)在这里
        // 国内网络可加阿里云镜像加速:
        // maven { url 'https://maven.aliyun.com/repository/public' }
    }
}
```

> 若项目仍在用旧式 `allprojects { repositories { ... } }`,把 `repositories { ... }` 内容照搬进去即可。

## 二、添加依赖

模块 `build.gradle`:

```groovy
dependencies {
    implementation 'com.energy.sdk:qx-hybrid:0.1.11'
}
```

Sync 即可。以下传递依赖会自动带入,无需手动声明:
`androidx.appcompat`、`com.google.android.material`、`androidx.core:core-ktx`、
`androidx.webkit:webkit`、`androidx.exifinterface`、`com.google.zxing:core`、
`com.journeyapps:zxing-android-embedded`、`com.github.aicareles:Android-BLE`、
`com.google.code.gson:gson`、`kotlin-stdlib`。

---

## 三、⚠️ 收到「SDK 已更新」通知后必须清缓存

维护者有时会**用同一个版本号重新发布**修复。这种情况下 Gradle 不会感知到变化 —— 它把静态版本号(如 `0.1.11`)视为不可变内容并永久缓存,**你构建时用的还是旧的 aar**。

收到更新通知后,任选一种方式:

```bash
./gradlew --refresh-dependencies
```

或直接删掉本地缓存目录:

```bash
rm -rf ~/.gradle/caches/modules-2/files-2.1/com.energy.sdk
```

> 另外 `raw.githubusercontent.com` 有约 5 分钟的 CDN 缓存,刚发布后立刻拉可能仍是旧的,等几分钟再试。

### 关于 `cacheChangingModulesFor`

有人会想用这个来自动拿到最新:

```groovy
// ❌ 单独写这个,对静态版本号完全无效
configurations.all {
    resolutionStrategy.cacheChangingModulesFor 0, 'seconds'
}
```

**它只对 changing module 生效**,而 `0.1.11` 这种静态版本不是 changing module,所以这段配置不起任何作用。要生效必须同时把依赖标记为 changing:

```groovy
implementation('com.energy.sdk:qx-hybrid:0.1.11') { changing = true }
```

代价是每次构建都会发网络请求校验,构建变慢。**日常接入不建议这么做**,按上面清缓存即可。

---

## 四、常见问题

- **拉不到包 / 404**:确认仓库地址结尾是 `/main`(不要带 `com/energy/sdk` 这类路径),且 `google()`、`mavenCentral()`、`jitpack` 都已配上。
- **只有 Android-BLE 拉不到**:你的网络访问不了公网 jitpack,联系维护者把该依赖镜像一份。
- **改了版本号还是旧行为**:见上面第三节,清 Gradle 缓存。
- **版本升级**:把坐标里的版本号换成维护者通知的新版本即可;当前可用版本见仓库内 `com/energy/sdk/qx-hybrid/maven-metadata.xml`。
